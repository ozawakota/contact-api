FROM python:3.11-slim

# 最小限の依存関係のみインストール
RUN pip install fastapi==0.104.1 uvicorn==0.24.0

WORKDIR /app

# 最小限のファイルのみコピー
COPY simple_app.py .

# 環境変数設定
ENV PYTHONUNBUFFERED=1

# 最小限起動コマンド
CMD ["python", "-m", "uvicorn", "simple_app:app", "--host", "0.0.0.0", "--port", "8080"]